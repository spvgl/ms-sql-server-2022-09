name: tsql_linter

on: push

jobs:
  build:
    name: Check code with TSQL Linter
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Run TSQLLint
        uses: lowlydba/tsqllint-action@v0.8.0
        with:
          path: "*.sql"
          config: "./.tsqllintrc"
          comment: true